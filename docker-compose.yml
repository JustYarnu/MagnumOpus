services:
  commit-bot:
    build: .
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_REPO=${GITHUB_REPO}
    volumes:
      - .:/app